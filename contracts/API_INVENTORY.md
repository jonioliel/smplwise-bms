# API inventory (generated)

Version 0.1.25 · 116 routes under `/api/v1` · generated by `scripts/api_inventory.py`. Every route sits behind the
Ingress identity and the scoped role check named in its handler; errors use the `ApiError` model
(`code`, `user_message`, `details`, `retryable`). Ids are 16-hex strings; instants are UTC ISO-8601 with `Z`.

| Router | Method | Path | Purpose |
|---|---|---|---|
| access | GET | `/api/v1/access/bindings` |  |
| access | POST | `/api/v1/access/bindings` |  |
| access | DELETE | `/api/v1/access/bindings/{binding_id}` |  |
| access | GET | `/api/v1/access/groups` |  |
| access | POST | `/api/v1/access/groups` |  |
| access | DELETE | `/api/v1/access/groups/{group_id}` |  |
| access | PUT | `/api/v1/access/groups/{group_id}/members` | Replace the membership. The actor must hold rbac.assign on every scope the group is bound to (§6). |
| access | POST | `/api/v1/access/preview` | Effective permissions of a user at a scope, computed server-side; no impersonation (§12). |
| access | GET | `/api/v1/access/roles` |  |
| access | GET | `/api/v1/audit` |  |
| access | POST | `/api/v1/identity/sync` | Ask the bridge integration to push the HA user directory now (it pushes every 60 s anyway). |
| access | GET | `/api/v1/identity/users` |  |
| anchors | GET | `/api/v1/floors/{floor_id}/anchors` |  |
| anchors | POST | `/api/v1/floors/{floor_id}/anchors` |  |
| anchors | GET | `/api/v1/floors/{floor_id}/map` | Everything the map needs in one call. `draft=true` (editors) prefers the latest draft background; `at=<UTC>` |
| anchors | DELETE | `/api/v1/map-anchors/{anchor_id}` |  |
| anchors | PATCH | `/api/v1/map-anchors/{anchor_id}` |  |
| backup | GET | `/api/v1/backups` |  |
| backup | POST | `/api/v1/backups` |  |
| backup | POST | `/api/v1/backups/upload` |  |
| backup | DELETE | `/api/v1/backups/{name}` |  |
| backup | GET | `/api/v1/backups/{name}` |  |
| backup | GET | `/api/v1/backups/{name}/download` |  |
| backup | POST | `/api/v1/backups/{name}/restore` |  |
| cameras | GET | `/api/v1/cameras` | Cameras the caller may see: everything for installation-wide readers, otherwise only cameras |
| cameras | POST | `/api/v1/cameras` |  |
| cameras | POST | `/api/v1/cameras/sync` | Read-only discovery from the NVR: channels, online flag and track ids. Existing aliases/order survive. |
| cameras | PATCH | `/api/v1/cameras/{camera_id}` |  |
| cameras | GET | `/api/v1/cameras/{camera_id}/snapshot.jpg` | Fresh JPEG from the NVR (read-only), cached in /data for `snapshots.max_age_s`; a stale copy is |
| cases | GET | `/api/v1/cases` |  |
| cases | POST | `/api/v1/cases` |  |
| cases | DELETE | `/api/v1/cases/{case_id}` |  |
| cases | GET | `/api/v1/cases/{case_id}` | The case with its items; `check=false` skips the NVR coverage probe (preservation then reads unknown). |
| cases | PATCH | `/api/v1/cases/{case_id}` |  |
| cases | POST | `/api/v1/cases/{case_id}/items` |  |
| cases | DELETE | `/api/v1/cases/{case_id}/items/{item_id}` |  |
| cases | POST | `/api/v1/cases/{case_id}/items/{item_id}/preserve` | Copy the footage out of the NVR through an export job (T048) and link it; only then the item counts as preserved. |
| catalog | DELETE | `/api/v1/buildings/{building_id}` |  |
| catalog | PATCH | `/api/v1/buildings/{building_id}` |  |
| catalog | POST | `/api/v1/buildings/{building_id}/floors` |  |
| catalog | DELETE | `/api/v1/floors/{floor_id}` | A floor with anchors is not deleted silently: 409 unless force=true, which tombstones the anchors first. |
| catalog | GET | `/api/v1/floors/{floor_id}` |  |
| catalog | PATCH | `/api/v1/floors/{floor_id}` |  |
| catalog | GET | `/api/v1/sites` | Only nodes the caller may read appear; a site is listed when the caller can read it or any child. |
| catalog | POST | `/api/v1/sites` |  |
| catalog | DELETE | `/api/v1/sites/{site_id}` |  |
| catalog | PATCH | `/api/v1/sites/{site_id}` |  |
| catalog | POST | `/api/v1/sites/{site_id}/buildings` |  |
| events | GET | `/api/v1/cameras/{camera_id}/events` | Timeline markers for one camera and local day (events.read or video.playback on that camera). |
| events | GET | `/api/v1/events` |  |
| events | POST | `/api/v1/events/ack-many` | Handle a whole window: every event is checked for scope like a single ack; each ack is audited by name. |
| events | GET | `/api/v1/events/summary` |  |
| events | GET | `/api/v1/events/windows` | Review windows (design M26): the day's events grouped per camera by proximity; nothing is deleted or merged |
| events | GET | `/api/v1/events/{event_id}` | One event with its picture state and the camera's place on the floor (event page, design M27). |
| events | POST | `/api/v1/events/{event_id}/ack` |  |
| events | GET | `/api/v1/events/{event_id}/thumbnail` | The event's picture from the recording: 200 image/jpeg when ready, 202 while it is being grabbed, |
| exports | GET | `/api/v1/exports` |  |
| exports | POST | `/api/v1/exports` |  |
| exports | POST | `/api/v1/exports/estimate` |  |
| exports | DELETE | `/api/v1/exports/{job_id}` |  |
| exports | GET | `/api/v1/exports/{job_id}` |  |
| exports | POST | `/api/v1/exports/{job_id}/cancel` |  |
| exports | GET | `/api/v1/exports/{job_id}/download` |  |
| exports | GET | `/api/v1/exports/{job_id}/manifest` |  |
| home-assistant | GET | `/api/v1/ha/actions/{action_id}` | Poll a pending action: confirmed when the entity reached the expected state after the request, unknown after 20 s. |
| home-assistant | POST | `/api/v1/ha/bridge/directory` | Signed user directory push from the integration (id, name, username, active, admin, groups). |
| home-assistant | POST | `/api/v1/ha/bridge/install` | Copy the integration into Home Assistant's config directory again and re-announce it (system.configure). |
| home-assistant | GET | `/api/v1/ha/bridge/pairing` |  |
| home-assistant | POST | `/api/v1/ha/bridge/ping` | Called by the integration during its config flow: proves the pairing code and marks the bridge paired. |
| home-assistant | GET | `/api/v1/ha/entities` | The catalogue: everything for installation-wide readers, only placed entities on visible floors otherwise. |
| home-assistant | GET | `/api/v1/ha/entities/{entity_id}` |  |
| home-assistant | POST | `/api/v1/ha/entities/{entity_id}/actions` |  |
| home-assistant | GET | `/api/v1/ha/status` |  |
| identity | GET | `/api/v1/me` |  |
| media | GET | `/api/v1/media/live/{camera_id}` | What the player needs before opening the socket: permission, transport default and the relay path. |
| media | GET | `/api/v1/media/sessions` |  |
| media | GET | `/api/v1/media/streams` |  |
| media | POST | `/api/v1/media/streams/sync` | Pre-create our namespaced streams in go2rtc for every enabled camera (idempotent, bounded). Also runs |
| ops | GET | `/api/v1/health` |  |
| ops | GET | `/api/v1/health/report` | One status per subsystem (T033). Device probes are network calls, so they run outside the request's |
| ops | GET | `/api/v1/health/summary` | What the top bar shows every signed-in user: cached states only, no device probes, no details that a |
| plans | GET | `/api/v1/floors/{floor_id}/plan-assets` |  |
| plans | POST | `/api/v1/floors/{floor_id}/plan-assets` |  |
| plans | GET | `/api/v1/floors/{floor_id}/plan-versions` |  |
| plans | POST | `/api/v1/floors/{floor_id}/plan-versions` |  |
| plans | GET | `/api/v1/plan-assets/{asset_id}/pages/{page}/preview.png` | Page preview for the import wizard; `rotation` (0/90/180/270) returns the page as the derived version |
| plans | DELETE | `/api/v1/plan-versions/{version_id}` |  |
| plans | GET | `/api/v1/plan-versions/{version_id}` |  |
| plans | PATCH | `/api/v1/plan-versions/{version_id}` |  |
| plans | GET | `/api/v1/plan-versions/{version_id}/diff` | Preview shown before publishing or restoring: geometry changes against the published version (or `against`) |
| plans | GET | `/api/v1/plan-versions/{version_id}/image.png` |  |
| plans | POST | `/api/v1/plan-versions/{version_id}/publish` |  |
| plans | POST | `/api/v1/plan-versions/{version_id}/rollback` | Restore an archived version as a new published version (its own copy of the picture and the same geometry), so |
| plans | GET | `/api/v1/plan-versions/{version_id}/source.png` |  |
| plans | POST | `/api/v1/plan-versions/{version_id}/stylize` | Local image processing (no AI, nothing leaves the add-on): walls and rooms in the SMPLWISE language. |
| plans | GET | `/api/v1/plan-versions/{version_id}/stylized.png` |  |
| playback | POST | `/api/v1/playback/groups` |  |
| playback | DELETE | `/api/v1/playback/groups/{group_id}` |  |
| playback | GET | `/api/v1/playback/groups/{group_id}` |  |
| playback | POST | `/api/v1/playback/groups/{group_id}/seek` |  |
| playback | GET | `/api/v1/playback/sessions` |  |
| playback | POST | `/api/v1/playback/sessions` |  |
| playback | DELETE | `/api/v1/playback/sessions/{session_id}` |  |
| playback | GET | `/api/v1/playback/sessions/{session_id}` |  |
| playback | POST | `/api/v1/playback/sessions/{session_id}/seek` |  |
| recordings | GET | `/api/v1/cameras/{camera_id}/frame` | JPEG frame from the camera's recording at `at` (UTC); 404 when the recording has no picture there. |
| recordings | GET | `/api/v1/cameras/{camera_id}/recordings` |  |
| search | GET | `/api/v1/search` |  |
| settings | GET | `/api/v1/settings` |  |
| settings | PATCH | `/api/v1/settings` |  |
| zones | GET | `/api/v1/floors/{floor_id}/zones` |  |
| zones | POST | `/api/v1/floors/{floor_id}/zones` |  |
| zones | POST | `/api/v1/floors/{floor_id}/zones/accept` | Save chosen candidates as zones (source 'auto'); optionally replace the previous automatic ones. |
| zones | POST | `/api/v1/floors/{floor_id}/zones/detect` | Room candidates from the floor's plan (local image processing; nothing is saved until accepted). |
| zones | DELETE | `/api/v1/zones/{zone_id}` |  |
| zones | PATCH | `/api/v1/zones/{zone_id}` |  |
