"""SMPLWISE VMS add-on backend (FastAPI + SQLite).

Server-side truth for the catalogue (sites → buildings → floors → plans → anchors), identity from the
Supervisor Ingress proxy, VMS-local roles and scoped bindings, audit, and read-only source adapters.
"""

__version__ = "0.1.64"
