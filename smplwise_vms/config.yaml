name: SMPLWISE VMS
version: "0.1.29"
slug: smplwise_vms
description: Map-centred video management for Hikvision NVRs and Home Assistant (pilot build)
url: https://github.com/jonioliel/smplwise-bms
arch:
  - amd64
  - aarch64
startup: application
boot: auto
init: false
ingress: true
homeassistant_api: true
# Supervisor API (default role) for discovery: the add-on announces the bridge integration it installs.
hassio_api: true
discovery:
  - smplwise_bridge
# The integration is copied into <config>/custom_components/smplwise_bridge; nothing else there is touched.
map:
  - type: homeassistant_config
    read_only: false
ingress_port: 8099
panel_icon: mdi:cctv
panel_title: SMPLWISE VMS
# Menu entry for every HA user; access inside the product is decided by VMS roles, never by this flag.
panel_admin: false
watchdog: http://[HOST]:[PORT:8099]/healthz
backup: hot
options:
  bootstrap_admin_username: ""
  nvr_host: ""
  nvr_http_port: 80
  nvr_rtsp_port: 554
  nvr_username: ""
  nvr_password: ""
  go2rtc_url: ""
  go2rtc_api_username: ""
  go2rtc_api_password: ""
  log_level: info
schema:
  bootstrap_admin_username: str
  nvr_host: str?
  nvr_http_port: port
  nvr_rtsp_port: port
  nvr_username: str?
  nvr_password: password?
  go2rtc_url: url?
  go2rtc_api_username: str?
  go2rtc_api_password: password?
  log_level: list(debug|info|warning|error)
