# סבב בדיקות עמוק — 24.9.2026 (גרסה 0.1.82, Plan Studio שלב 1)

מטרה: לבקשת הבעלים אחרי שחרור 0.1.82 ("תערוך סבב בדיקות עמוק שאתה יכול לערוך ותעדכן את מסמך הבדיקות") — להריץ כל
מה שניתן להריץ מתחנת העבודה, לחפש רגרסיות בכל המערכת ולא רק במה שהגרסה הוסיפה, לבדוק את מסלול השדרוג האמיתי
ממסד נתונים של גרסה קודמת, לדחוף את ה־API החדש לקצוות (גודל, מקביליות, קלט שגוי, הרשאות), ולעבור על המסכים החדשים
בדפדפן. המסמך הזה הוא הרישום המלא של מה הורץ, מה נמצא ומה תוקן. הסבב הקודם (0.1.79–0.1.81) נמצא
ב־`TEST_ROUND_RESULTS_2026-09-22_HE.md`; הצ'קליסט של הבעלים לגרסה הזאת הוא `PLAN_STUDIO_PHASE1_CHECKLIST_HE.md`.

## תנאי הסביבה בזמן הסבב

- קוד: `main` = `g0/intake` = מיזוג 06d3022 (הענף `pilot/T084-plan-studio-1`, 35 commits, כולל תיקוני ביקורת הענף השלמה
  ב־02858a3). שרת הפיתוח על תחנת העבודה מגיש את ה־`dist` הבנוי של אותו commit; ה־`www` שבתוך ה־add-on נבנה מאותו מקור.
- **ה־NVR וה־HA של הבעלים לא היו נגישים מהתחנה** (ConnectTimeout על ISAPI, TimeoutError על ה־WebSocket של HA — כמו
  ב־22.9). כל מה שדורש מכשיר חי מסומן BLOCKED, לא "עבר" ולא "נכשל". מאותה סיבה "בדוק עדכון" בחנות לא הופעל מכאן.
- בסיס הנתונים של הפיתוח לא קיבל אירועים חדשים (אין NVR), ולכן בדיקות שמחפשות "אירוע מהיממה האחרונה" לא מצאו כלום.
- לבדיקת השדרוג והקצוות הוקם **מופע זמני נפרד** (backend נוסף על פורט אחר, ספריית נתונים משלו בספריית הזמניים של
  הסשן, ללא NVR/HA), כדי לא לגעת בנתוני הפיתוח ולא בשום מכשיר. המופע, ה־worktree של הקוד הישן וספריית הנתונים נמחקו
  בסוף הסבב. צילומי המסך שנלקחו שם מכילים את תוכניות הבעלים ולכן **לא נכנסו למאגר**; מה שהם מראים מתועד במילים.

## 1. חבילות בדיקה אוטומטיות

| חבילה | היקף | תוצאה |
|---|---|---|
| Backend (`pytest`, כל החבילה) | 239 בדיקות (77 קבצים) | ✔ 239 עברו (5:29 דק׳, 2 אזהרות ידועות — operation id כפול ב־`realign_anchors`, קיים מלפני הענף, יש כרטיס לניקוי) |
| קובץ הזהב של הפרימיטיבים (`scripts/geometry_golden.py --check`) | Python ↔ TypeScript | ✔ תקין (exit 0) |
| TypeScript (`tsc --noEmit`) | כל ה־frontend | ✔ נקי |
| בדיקות יחידה ב־node (`unit-geometry`, `unit-studio-controller`) | 19 | ✔ 19 עברו (5 שנ׳) |
| מפרט חי של הסטודיו (`evidence-plan-studio.spec.ts`, Chrome אמיתי) | 4 | ✔ 4 עברו (30 שנ׳) — פעם שלישית על אותו עץ |
| שרשרת ה־fixtures (דמו, RTL, ללא גלילה הצידה, 1440/1024/390) | 129 | ✔ 129 עברו (1.2 דק׳) |
| **Sweep חי מלא** (`evidence-*.spec.ts`, 90 בדיקות, worker אחד, Chrome אמיתי, 18.6 דק׳) | כל ה־specs נגד שרת הפיתוח | 52 עברו · 37 נכשלו · 1 דילוג. מתוך ה־37: **36 BLOCKED** על מכשירים (אותה קבוצה בדיוק כמו ב־22.9) ו־**1 בדיקה שהתיישנה** (`evidence-viewer`) — תוקנה ורצה שוב: ✔ 1/1. פירוט ב־1.1 |
| `scripts/release_check.py` | עקביות השחרור | ✔ גרסה אחת בכל מקום (0.1.82), עותקי ה־bridge זהים, 19 מיגרציות רציפות, `www` קיים; רק התג `v0.1.82` עדיין לא נוצר (נוצר אחרי אישור הבעלים) |
| `scripts/smoke_after_upgrade.py --base` (16 בדיקות קריאה נגד שרת הפיתוח) | | 14 ✔ · 2 ✘ — `playback` ו־`export estimate` מחזירים 503 `source_unavailable`: ה־NVR. `--live` (ההתקנה של הבעלים) BLOCKED — ה־WebSocket של HA לא ענה |
| `scripts/load_probe.py` (8 עובדים × 3 סבבים, GETs שהממשק מבצע) | | ✔ p95: מפת קומה 163 ms · מפה ברגע נתון 172 ms · אירועים 157 ms · facets 185 ms · חיפוש 291 ms · מצלמות 417 ms · תיקים 279 ms · אחסון (cache) 57 ms; `camera recordings today` — 24 שגיאות (NVR) |

### 1.1 ה־sweep החי — פירוט 37 הכישלונות

| קבוצה | כמה | אילו בדיקות | מה זה אומר |
|---|---|---|---|
| BLOCKED — אין אירועים טריים | 14 | cases, correlation, event-detail, event-windows, events, events-hover, history-map, route, rules, spatial-search, speeds, owner-round2, owner-round5, review-fixes | הבדיקה עוצרת על "אירוע עם מצלמה מהיממה האחרונה" — אין כזה |
| BLOCKED — דורש תשובה מה־NVR / go2rtc | 19 | capabilities, frames, bundle, nvr-motion, nvr-notify, nvr-record, owner-round8 ×2, owner-round9, owner-round11, storage, zones-nvr, sync, video ×6 | `source_unavailable · ConnectTimeout` |
| BLOCKED — דורש HA מחובר | 3 | ha ×2, ha-history | `sync.connected=false` |
| **בדיקה שהתיישנה — תוקנה בסבב הזה** | 1 | `evidence-viewer` › layers panel: ציפתה ל־5 שורות בפאנל השכבות; מאז 0.1.82 יש 6 — השורה "מבנה" (שם השכבה מהאפיון, מוצגת תמיד, עם "N קירות · M פתחים" או "לא פורסם מבנה"). הבדיקה מצפה עכשיו ל־6 ולטקסט "מבנה"; רצה שוב לבד: ✔ | לא באג במוצר — השכבה נוספה בכוונה (החלטה R-T11-3) |

לעומת 22.9 (42 עברו · 43 נכשלו): שבע הבדיקות שתוקנו אז עוברות עכשיו, ולא נוספה אף בדיקה חסומה חדשה.

## 2. מסלול השדרוג האמיתי: 0.1.81 → 0.1.82 על מסד נתונים מאוכלס

זה מה שיקרה בהתקנה של הבעלים כשילחץ "עדכן", ולכן נבדק בנפרד מבדיקות היחידה (שמריצות את המיגרציות על מסד ריק):

1. הקוד של 0.1.81 (commit 5c0ffe8, ב־worktree זמני) הורץ על ספריית נתונים ריקה → 17 מיגרציות, `health` תקין.
2. **הגיבוי הידני מ־22.9 של הבעלים** (`app_version 0.1.78`, 18 טבלאות: 24 גרסאות תוכנית, 27 עוגנים, 21 אזורים, 7 משתמשים,
   74 שיוכים, 45 קבצי תוכניות) שוחזר לתוכו במצב replace + access → 8 אתרים, 10 קומות; `permission_revision` עלה.
3. השרת נעצר; **0.1.82 הופעל על אותה ספריית נתונים** → ביומן: `applied migrations [18, 19]`; `health` 0.1.82 תקין;
   `plan_geometry` ריקה (כצפוי), עמודות ה־level נוספו ל־`spatial_zones` ול־`map_anchors`, כל 24 הגרסאות ו־27 העוגנים במקומם.
4. על הנתונים הישנים, דרך ה־API החדש (קומה עם גרסה מפורסמת ו־2 עוגנים):

| מה | תוצאה |
|---|---|
| חבילת המפה של קומה ישנה | ✔ `geometry: null`, `permissions.structure: true` למנהל; המסכים לא נשברים כשאין מבנה |
| `GET …/geometry?draft=true` על גרסה ישנה | ✔ טיוטה ריקה (revision 0, status `new`) עם מידות הגרסה וכיול `missing` |
| `PUT` טיוטה (2 קירות, דלת, תווית) → `PUT` עם revision ישן | ✔ 200 · ✔ 409 `stale_revision` |
| `PUT` עם `walls: "x"`, נקודה לא מספרית, NaN, `meta` בעומק 80 | ✔ 422 `geometry_structure` לכולם, בלי שמירה |
| השוואה → פרסום → פרסום חוזר | ✔ diff עם ספירות · ✔ פורסם · ✔ `unchanged: true` בפעם השנייה (ללא רשומת audit נוספת) |
| ציר זמן, רשימת גרסאות, החבילה אחרי הפרסום | ✔ רשומה אחת · ✔ הפניה עם hash, revision ו־published_at |
| החבילה `?at=` לפני הפרסום / לפני `history_from` | ✔ אין מבנה לפני הפרסום; לפני תחילת ההיסטוריה — המבנה הנוכחי (כמתוכנן); אחרי פרסום שני — הגרסה שהייתה בתוקף אז מוחזרת כ־`archived` |
| ייצוא SVG / PNG / PNG ללא רקע | ✔ 200 (2.2 KB ב־20 ms · 1.1 MB ב־0.7 שנ׳ · 14 KB ב־0.15 שנ׳); תווית עם `<b>&amp;</b>` יוצאת מוברחת |
| כיול בזוג נקודות על גרסה ישנה | ✔ `scale_m_per_px` נכון (16 מ׳ / 0.8×רוחב), `residual 0`, `measured`; `plan.scale_m_per_px` בחבילה מתעדכן מיד, בלוק הכיול במסמך ממתין לפרסום |
| מטריצת הרשאות (משתמשים ששוחזרו מהגיבוי): `guard` (צופה) | ✔ מפה, ציר זמן ו־SVG מפורסם 200; טיוטה, diff, גרסאות, PUT, פרסום, rollback, copy-from, כיול, ייצוא טיוטה — 403 |
| `nobody` (ללא גישה) · `dana` (עורכת קומה אחרת) | ✔ 403 על הכול |
| גרסה חדשה מאותו שרטוט בחיתוך אחר | ✔ `geometry_carry: transformed`, הטיוטה מכילה את 2 הקירות, הדלת והתווית (בתוך החיתוך), כיול נישא (`measured`) |
| פרסום ישיר של הגרסה (המסלול של אשף הייבוא) | ✔ הגרסה והמבנה הנישא מתפרסמים יחד — **בלי תצוגה מקדימה של המבנה** (ההערה הידועה R-F-2, לסבב ההקשחה) |
| גיבוי חדש → שחזור merge → שחזור **replace עם הגיבוי הישן (0.1.78)** → שחזור replace עם החדש | ✔ הארכיון מכיל `plan_geometry.json` (7 שורות) · ✔ merge לא מכפיל · ✔ הארכיון הישן מרוקן את טבלת המבנה (ההתנהגות שתועדה ב־CHANGELOG) · ✔ החדש מחזיר את כל 7 השורות ואת ציר הזמן |
| מחיקת גרסת טיוטה עם מבנה | ✔ 204, שורות המבנה שלה נמחקות |

## 3. קצוות, גודל, מקביליות וקלט עוין (על המופע הזמני)

| מה | תוצאה |
|---|---|
| מסמך בגודל המרבי: 2,000 קירות, 4,000 פתחים, 1,000 תוויות (1.4 MB) | ✔ PUT 1.05 שנ׳ · GET 0.56 שנ׳ (1.3 MB) · diff 0.22 שנ׳ · SVG 0.40 שנ׳ (1.1 MB) · PNG 0.92 שנ׳ · PNG ללא רקע 0.47 שנ׳; 0 בעיות ולידציה |
| 2,001 קירות | ✔ 422 `limit` תוך 0.14 שנ׳ (הבדיקה עוצרת לפני המעבר על הפריטים) |
| 10 שמירות מקבילות על אותו revision | ✔ בדיוק אחת 200, תשע 409; ה־revision עלה באחד והטיוטה מכילה רק את המנצחת |
| `?at=garbage`, תאריך לא חוקי, תאריך בלי שעה | ✔ 422 `validation`; קצוות (`0001-01-01`, `9999-12-31`, אזור זמן +03:00) — 200 |
| מזהים בצורת הזרקה / traversal / 300 תווים | ✔ 404 `not_found` |
| תווי בקרה (`\x07`, `\t`) בתווית → ייצוא SVG | נשמר; ה־SVG **אינו XML תקין** (ידוע — Minor 5 של ביקורת הענף, בסבב ההקשחה של שלב 2; לא הזרקה: `<&>"` מוברחים) |
| קיר מחוץ לגבולות + פתח ברוחב 900 מ׳ בטיוטה | נשמר עם שגיאות (`bounds`, ו־`unknown_wall` לפתח — הקיר שמחוץ לגבולות לא נספר ככזה; שתי שגיאות לסיבה אחת, קוסמטי); ייצוא הטיוטה 200 (SVG 1.7 KB, PNG 0.6 שנ׳, בלי 500); פרסום גרסת התוכנית — ✔ 422 `geometry_invalid` |
| פרסום מבנה על גרסת תוכנית בטיוטה | ✔ 409 `publish_plan_first` — הפרסום עובר דרך פרסום הגרסה |
| יומני שלושת השרתים (פיתוח, ישן, זמני) אחרי הכול | ✔ 0 tracebacks · 0 שורות ERROR · 0 תשובות 5xx |

## 4. מעבר בדפדפן על המסכים החדשים (Chromium, 1440×900 ואז 390×844, על המופע הזמני עם הנתונים הישנים)

| מסך | מה נבדק | תוצאה |
|---|---|---|
| מפת קומה חיה | המבנה מצויר (3 מקטעי קיר, פתח); פאנל "שכבות" | ✔ 6 שורות: מצלמות, דלתות ואינטרקום, תאורה, אבטחה וחיישנים, שמות חדרים, **מבנה — 2 קירות · 1 פתחים**; כיבוי השכבה מוריד רק את הקירות (0), הדלקה מחזירה (3) |
| עורך התוכנית — כלי המבנה | פאנל: בחירה/קיר/דלת/חלון/מעבר/תווית, הסבר הציור, סוג וחוזק קיר, "קנה מידה: 1 מ׳ = 105.0 פיקסלים", ספירות ביחיד/רבים נכונות ("2 קירות · פתח אחד · תווית אחת"), כפתורי JSON/PNG/SVG | ✔ הקירות מצוירים עם קשת הדלת; העורך נפתח על טיוטת הגרסה האחרונה ולכן הכותרת "פרסום גרסה" (נכון) |
| כלי הכיול | "כיול קנה מידה", מצב "מכויל: 1 מ׳ = 105.0 פיקסלים", שלושה צעדים, שדה מרחק, "שמור כיול" מושבת עד שתי נקודות, הערה שהכיול לא מזיז פריטים | ✔ |
| כלי המדידה | פאנל המדידה נפתח, ללא ≈ אחרי כיול | ✔ |
| מפה היסטורית | `t` אחרי הפרסום הראשון · `t` לפני כל פרסום | ✔ 3 מקטעים · ✔ 0 |
| **עמוד אירוע** (אירוע סינתטי שהוזרק למופע הזמני, מצלמה שמעוגנת על הקומה) | כרטיס המפה בעמוד האירוע | ✔ המבנה שהיה בתוקף בזמן האירוע מצויר (3 מקטעים, פתח) עם המצלמה והמנעול — הרגל שחסרה ל־AT168 עד עכשיו; הווידאו מציג "ההקלטה לא נפתחה" כי אין go2rtc (צפוי) |
| מערכת › כללי › לשונית "וידאו ומדיה" — "מידות לפני כיול" | השורה, הערך וברירת המחדל | ✔ הערך `true`, האפשרויות "משוערות עם ≈" / "מוסתרות עד כיול", הסבר השורה נכון; השורה יושבת בלשונית "וידאו ומדיה" (לא בלשונית "כללי" שנפתחת ראשונה) — הצ'קליסט של הבעלים מציין את הלשונית |
| טלפון (390×844) — העורך עם כלי המבנה, ומפת הקומה | `scrollWidth == clientWidth` | ✔ 390/390 בשניהם; סרגל הכלים והפאנלים נערמים |
| קונסולת הדפדפן | שגיאות | רק 503 על משאבי NVR (צפוי); 0 שגיאות JS; 0 `console.log` בקוד ה־frontend |

השורה `plan.estimates` נבדקה גם דרך ה־API ובבדיקה החיה של הסטודיו (שמצמידה את ההגדרה ומחזירה אותה בסוף).

## 5. מה נמצא

| מה | חומרה | מה נעשה |
|---|---|---|
| `evidence-viewer` ציפתה ל־5 שורות בפאנל השכבות (6 מאז 0.1.82) | בדיקה שהתיישנה | תוקנה בסבב הזה (מצפה ל־6 ולמילה "מבנה"); רצה שוב לבד — עברה |
| SVG עם תווי בקרה בתווית אינו XML תקין | ידוע (ביקורת הענף, Minor 5) | בסבב ההקשחה של שלב 2 — כלל ולידציה לתווי בקרה |
| אשף הייבוא מפרסם גרסה מיובאת ישירות, ומאז 0.1.82 גם את המבנה הנישא, בלי תצוגה מקדימה של המבנה | ידוע (R-F-2) | בראש סבב ההקשחה; עד אז — ייבוא מחדש של שרטוט שכבר פורסם מפרסם מבנה חתוך שאפשר לתקן בעורך |
| קיר מחוץ לגבולות מייצר גם `unknown_wall` לפתחים שלו | קוסמטי | נרשם לסבב ההקשחה (שגיאה אחת לסיבה אחת) |
| `?at=` בתאריך בלי שעה נדחה ב־422 | לא באג | הממשק שולח תמיד תאריך־שעה מלא |

לא נמצא אף באג חדש במוצר בסבב הזה; 0.1.82 שנדחפה ל־`main` לא שונתה (רק הבדיקה והמסמך הזה נוספו).

## 6. מה לא נגעתי בו, בכוונה

- כל מה שדורש NVR / go2rtc / HA חי (36 הבדיקות החסומות, `smoke --live`, "בדוק עדכון" בחנות): מצב הרשת, לא הקוד.
- **כרטיס Lovelace** עם שכבת המבנה — דורש HA; הכרטיס מטמיע את אותו מסך קומה שנבדק כאן, אבל בלי HA אין איך לפתוח אותו,
  ולכן AT168 נשאר "חלקי" — הרגל היחידה שנותרה.
- וידאו ב־Chrome אמיתי (H.264) — דורש go2rtc.
- הרשאה ברמת מצלמה בודדת (T055), אשף ההתקנה (T071), Visual regression ב־CI (T074) — כמו ב־22.9, החלטות שלך.

## 7. מה מחכה לך

הטופס לסבב 7 (20 סעיפים + 4 החלטות, נשמר אוטומטית): https://claude.ai/artifact/1sbsiVePW2JvoGnuS8qxZR

1. "בדוק עדכון" בחנות ההרחבות והתקנת 0.1.82 (מכאן זה נכשל על ה־WebSocket של HA).
2. הצ'קליסט של 16 השורות (`PLAN_STUDIO_PHASE1_CHECKLIST_HE.md`) על ההתקנה שלך — בעיקר מה שרק שם אפשר: כרטיס Lovelace עם
   שכבת המבנה, עמוד אירוע אמיתי, המפה ההיסטורית מתוך אירוע.
3. שלוש ההחלטות הפתוחות מהדוח הקודם (T054/T058, CI, מסלול ה־AI) ושתי ההערות של הגרסה: שם השכבה "מבנה" ותפקיד מותאם עם
   `map.publish` בלי `map.edit`.
